security_policy = ''''
Below is the security policy for the application:
Patient Names must never be exposed.
Patient secret pass codes, a three digit number must never be exposed.
Patient Addresses must never be exposed.
'''